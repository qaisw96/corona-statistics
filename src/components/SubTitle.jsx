import React from 'react';

const SubTitle = ({ children }) => (
  <div className='bg-gray-100 m-0 w-full text-slate-700'>
    <h1 className='text-2xl'>{children}</h1>
  </div>
);

export default SubTitle;
